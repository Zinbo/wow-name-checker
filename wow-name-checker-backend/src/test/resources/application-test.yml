spring:
  application:
    name: wow-name-checker
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:wownamecheckerdb;MODE=MYSQL
    username: sa
    driverClassName: org.h2.Driver

management:
  tracing:
    enabled: false

alerts:
  schedule: "* */5 * * * *"
